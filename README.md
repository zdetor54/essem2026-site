# essem2026-site

Set commit identity per repo (important)

Inside each repo:
git config --unset-all user.name
git config user.name "zdetor54"
git config --unset-all user.email
git config user.email "zdetor54@gmail.com"

git config user.name
git config user.email

another